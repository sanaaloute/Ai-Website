# Lovecode Admin — Environment Variables

# Backend API base URL (no trailing slash).
# Next.js rewrites /api/admin/* requests to this backend.
# This is required; there are no local mock API routes anymore.
BACKEND_API_URL=http://localhost:4000

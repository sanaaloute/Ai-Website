import { AgentState } from '../state';
import { GraphDependencies } from '../graph';
export declare function componentSelectorNode(state: AgentState, deps: GraphDependencies): Promise<Partial<AgentState>>;

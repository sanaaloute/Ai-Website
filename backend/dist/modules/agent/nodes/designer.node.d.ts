import { AgentState } from '../state';
import { GraphDependencies } from '../graph';
export declare function designerNode(state: AgentState, deps: GraphDependencies): Promise<Partial<AgentState>>;

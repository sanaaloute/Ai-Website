// Generated by synth-next-template.mjs from db_schema.json
// Do not edit by hand; re-run the generator to refresh.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid())
  email String @unique
  password String
  name String?
  role String?
  phone String?
  address String?
  reservations_user Reservation[] @relation("Rel_reservations_user")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("users")
}

model Category {
  id String @id @default(cuid())
  name String
  slug String @unique
  menu_items_category MenuItem[] @relation("Rel_menu_items_category")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("categories")
}

model MenuItem {
  id String @id @default(cuid())
  name String
  slug String @unique
  price Float
  description String?
  image String?
  status String
  categoryId String?
  category Category? @relation("Rel_menu_items_category", fields: [categoryId], references: [id])
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("menu_items")
}

model Reservation {
  id String @id @default(cuid())
  userId String
  user User @relation("Rel_reservations_user", fields: [userId], references: [id])
  partySize Int
  date String
  status String
  notes String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("reservations")
}

model Setting {
  id String @id @default(cuid())
  name String
  tagline String?
  footerText String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("settings")
}

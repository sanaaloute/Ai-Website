// Generated by synth-next-template.mjs from db_schema.json
// Do not edit by hand; re-run the generator to refresh.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid())
  email String @unique
  password String
  name String?
  role String?
  phone String?
  address String?
  messages_user Message[] @relation("Rel_messages_user")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("users")
}

model Category {
  id String @id @default(cuid())
  name String
  slug String @unique
  entries_category Entry[] @relation("Rel_entries_category")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("categories")
}

model Entry {
  id String @id @default(cuid())
  title String
  slug String @unique
  summary String?
  content String?
  image String?
  status String
  categoryId String?
  category Category? @relation("Rel_entries_category", fields: [categoryId], references: [id])
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("entries")
}

model Message {
  id String @id @default(cuid())
  userId String?
  user User? @relation("Rel_messages_user", fields: [userId], references: [id])
  name String
  email String
  body String
  status String
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("messages")
}

model Setting {
  id String @id @default(cuid())
  name String
  tagline String?
  supportEmail String?
  footerText String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("settings")
}

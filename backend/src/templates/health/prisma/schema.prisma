// Generated by synth-next-template.mjs from db_schema.json
// Do not edit by hand; re-run the generator to refresh.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid())
  email String @unique
  password String
  name String?
  role String?
  phone String?
  address String?
  appointments_user Appointment[] @relation("Rel_appointments_user")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("users")
}

model Category {
  id String @id @default(cuid())
  name String
  slug String @unique
  services_category Service[] @relation("Rel_services_category")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("categories")
}

model Service {
  id String @id @default(cuid())
  name String
  slug String @unique
  provider String?
  price Float?
  description String?
  image String?
  status String
  categoryId String?
  category Category? @relation("Rel_services_category", fields: [categoryId], references: [id])
  appointments_service Appointment[] @relation("Rel_appointments_service")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("services")
}

model Appointment {
  id String @id @default(cuid())
  userId String
  user User @relation("Rel_appointments_user", fields: [userId], references: [id])
  serviceId String?
  service Service? @relation("Rel_appointments_service", fields: [serviceId], references: [id])
  status String
  notes String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("appointments")
}

model Setting {
  id String @id @default(cuid())
  name String
  tagline String?
  footerText String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("settings")
}

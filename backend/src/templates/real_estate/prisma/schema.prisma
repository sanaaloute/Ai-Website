// Generated by synth-next-template.mjs from db_schema.json
// Do not edit by hand; re-run the generator to refresh.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid())
  email String @unique
  password String
  name String?
  role String?
  phone String?
  address String?
  inquiries_user Inquiry[] @relation("Rel_inquiries_user")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("users")
}

model Category {
  id String @id @default(cuid())
  name String
  slug String @unique
  properties_category Property[] @relation("Rel_properties_category")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("categories")
}

model Property {
  id String @id @default(cuid())
  name String
  slug String @unique
  location String?
  price Float?
  bedrooms Int?
  description String?
  image String?
  status String
  categoryId String?
  category Category? @relation("Rel_properties_category", fields: [categoryId], references: [id])
  inquiries_property Inquiry[] @relation("Rel_inquiries_property")
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("properties")
}

model Inquiry {
  id String @id @default(cuid())
  userId String
  user User @relation("Rel_inquiries_user", fields: [userId], references: [id])
  propertyId String?
  property Property? @relation("Rel_inquiries_property", fields: [propertyId], references: [id])
  status String
  message String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("inquiries")
}

model Setting {
  id String @id @default(cuid())
  name String
  tagline String?
  footerText String?
  created DateTime @default(now())
  updated DateTime @updatedAt
  @@map("settings")
}
